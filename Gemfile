# frozen_string_literal: true

# 指定 gem 的来源
source "https://mirrors.tuna.tsinghua.edu.cn/rubygems"

# 告诉 Bundler，这个项目本身就是一个 gem。
# Bundler 会自动读取 RM-Toolkit.gemspec 文件来找出所有依赖项
# (包括运行时和开发时依赖)，并注册可执行文件。
# 这是解决问题的关键。
gemspec